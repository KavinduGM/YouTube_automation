# Dokploy-friendly stack: migrator + API + Worker + Dashboard.
#
# Postgres is provided externally (Dokploy database service) — set
# DATABASE_URL in Dokploy's Environment tab.
#
# TLS + domains: handled by Dokploy's built-in Traefik. Assign domains
# to the dashboard (port 3000) and api (port 4000) services in the
# Domains tab.
#
# IMPORTANT: env vars are NOT declared here. Dokploy injects everything
# from its Environment tab into every service in this stack. If you run
# this compose outside Dokploy, you need to either (a) export the env
# vars in your shell before `docker compose up`, or (b) add an
# `env_file: .env` line under each service.

services:
  # One-shot job: syncs DB schema, then exits 0.
  migrator:
    build: .
    image: yt-automation
    pull_policy: never
    command: ["npx", "prisma", "db", "push", "--accept-data-loss", "--skip-generate"]
    restart: "no"

  api:
    image: yt-automation
    pull_policy: never
    command: ["node", "apps/api/dist/index.js"]
    expose:
      - "4000"
    depends_on:
      migrator:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 5

  worker:
    image: yt-automation
    pull_policy: never
    command: ["node", "apps/worker/dist/index.js"]
    volumes:
      - workertmp:/app/tmp
    depends_on:
      migrator:
        condition: service_completed_successfully
    restart: unless-stopped

  dashboard:
    image: yt-automation
    pull_policy: never
    working_dir: /app/apps/dashboard
    command: ["node", "/app/node_modules/next/dist/bin/next", "start", "-p", "3000"]
    expose:
      - "3000"
    depends_on:
      api:
        condition: service_started
    restart: unless-stopped

volumes:
  workertmp:
